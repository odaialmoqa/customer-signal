import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CustomerSignal - Advanced Customer Feedback Analytics",
  description: "Monitor online conversations and customer feedback across multiple platforms with advanced sentiment analysis and insights.",
};

// Initialize monitoring system
if (typeof window === 'undefined') {
  // Server-side initialization
  import('../lib/monitoring/init').then(({ initializeMonitoring }) => {
    initializeMonitoring();
  }).catch(console.error);
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
